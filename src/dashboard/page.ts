// Local-only Argus dashboard. Zero dependencies, zero build step.
// The UI is intentionally an operations console: triage first, investigate second.

export function renderPage(): string {
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<meta name="color-scheme" content="dark" />
<title>ARGUS // on-chain intelligence</title>
<style>
:root{--bg:#070a0d;--surface:#0d1217;--surface-2:#111922;--surface-3:#16212b;--line:#20303a;--line-bright:#2b414d;--text:#e5edf0;--muted:#8498a1;--quiet:#52666f;--mint:#57e6ad;--amber:#f5b84b;--red:#ff6b6b;--blue:#72b8ff;--violet:#b899ff;--sans:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;--mono:"SFMono-Regular",Consolas,"Liberation Mono",monospace;--shadow:0 18px 60px rgba(0,0,0,.24)}
*{box-sizing:border-box}html{background:var(--bg);scroll-behavior:smooth}body{margin:0;color:var(--text);font:14px/1.5 var(--sans);background:radial-gradient(900px 500px at 72% -10%,#112631 0,transparent 66%),var(--bg);-webkit-font-smoothing:antialiased}body:before{content:"";position:fixed;inset:0;pointer-events:none;opacity:.22;background-image:linear-gradient(rgba(110,170,180,.06) 1px,transparent 1px),linear-gradient(90deg,rgba(110,170,180,.06) 1px,transparent 1px);background-size:48px 48px;mask-image:linear-gradient(#000,transparent 75%)}button,input{font:inherit}button{cursor:pointer}.skip{position:absolute;top:-50px;left:12px;background:var(--mint);color:#06100c;padding:10px 14px;z-index:5}.skip:focus{top:12px}
.app{display:grid;grid-template-columns:224px minmax(0,1fr);min-height:100vh}.sidebar{position:sticky;top:0;height:100vh;padding:25px 16px 18px;border-right:1px solid var(--line);background:rgba(8,12,16,.82);backdrop-filter:blur(18px);display:flex;flex-direction:column}.brand{display:flex;align-items:center;gap:11px;padding:0 10px 33px}.eye{width:31px;height:31px;border:1px solid var(--mint);border-radius:50%;position:relative;box-shadow:0 0 25px rgba(87,230,173,.15)}.eye:before{content:"";position:absolute;inset:7px;border:1px solid var(--mint);border-radius:50%}.eye:after{content:"";position:absolute;width:5px;height:5px;left:12px;top:12px;border-radius:50%;background:var(--mint)}.brand strong{display:block;font-size:18px;letter-spacing:.27em}.brand small{display:block;color:var(--quiet);font:9px var(--mono);letter-spacing:.16em;text-transform:uppercase;margin-top:2px}.nav-label{padding:0 11px 8px;color:var(--quiet);font:10px var(--mono);letter-spacing:.16em;text-transform:uppercase}.nav{display:grid;gap:4px}.nav button{border:1px solid transparent;background:transparent;color:var(--muted);text-align:left;border-radius:7px;padding:11px 11px;display:flex;align-items:center;gap:11px;transition:.18s ease}.nav button:hover{background:var(--surface);color:var(--text)}.nav button.active{background:linear-gradient(90deg,rgba(87,230,173,.13),transparent);border-color:rgba(87,230,173,.25);color:var(--mint)}.nav i{width:16px;color:inherit;font-style:normal;text-align:center;font-family:var(--mono)}.side-bottom{margin-top:auto;padding:14px 10px 0;border-top:1px solid var(--line);color:var(--quiet);font:10px/1.7 var(--mono)}.side-bottom b{color:var(--muted);font-weight:400}.main{min-width:0}.topbar{height:76px;padding:16px 34px;display:flex;align-items:center;justify-content:space-between;gap:20px;border-bottom:1px solid var(--line);background:rgba(7,10,13,.62);backdrop-filter:blur(18px);position:sticky;top:0;z-index:3}.crumb{display:flex;align-items:center;gap:11px}.eyebrow{font:10px var(--mono);letter-spacing:.16em;text-transform:uppercase;color:var(--quiet)}.page-title{font-size:20px;font-weight:650;letter-spacing:-.02em}.top-actions{display:flex;align-items:center;gap:10px}.status{display:flex;align-items:center;gap:8px;padding:8px 11px;border:1px solid var(--line);border-radius:6px;color:var(--muted);font:11px var(--mono)}.dot{width:7px;height:7px;border-radius:50%;background:var(--quiet)}.status.live .dot{background:var(--mint);box-shadow:0 0 12px var(--mint)}.status.stale .dot{background:var(--amber)}.status.down .dot{background:var(--red)}.icon-btn,.filter{border:1px solid var(--line);background:var(--surface);color:var(--muted);border-radius:6px;padding:8px 11px}.icon-btn:hover,.filter:hover{border-color:var(--line-bright);color:var(--text)}.content{max-width:1500px;margin:0 auto;padding:31px 34px 60px}.view{display:none}.view.active{display:block;animation:rise .3s ease both}@keyframes rise{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}.view-head{display:flex;justify-content:space-between;align-items:end;gap:18px;margin-bottom:24px}.view-head h1{margin:5px 0 0;font-size:29px;letter-spacing:-.04em;font-weight:650}.view-head p{margin:8px 0 0;color:var(--muted);max-width:600px}.actions{display:flex;gap:8px;align-items:center}.section-label{font:10px var(--mono);letter-spacing:.16em;text-transform:uppercase;color:var(--quiet);margin-bottom:11px}.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:28px}.metric{min-height:104px;padding:16px 17px;border:1px solid var(--line);background:linear-gradient(145deg,rgba(20,31,40,.9),rgba(12,17,22,.9));border-radius:8px;position:relative;overflow:hidden}.metric:after{content:"";position:absolute;right:-24px;bottom:-45px;width:110px;height:110px;border:1px solid rgba(87,230,173,.08);border-radius:50%}.metric .label{display:block;color:var(--muted);font:10px var(--mono);letter-spacing:.12em;text-transform:uppercase}.metric strong{display:block;margin-top:11px;font:27px var(--mono);letter-spacing:-.07em}.metric .delta{display:block;margin-top:3px;color:var(--quiet);font-size:11px}.metric.alert strong{color:var(--amber)}.metric.live strong{color:var(--mint)}.split{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(300px,.75fr);gap:18px}.panel{border:1px solid var(--line);background:rgba(13,18,23,.9);border-radius:8px;box-shadow:var(--shadow);overflow:hidden}.panel-head{padding:16px 18px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line)}.panel-head h2{margin:0;font-size:14px;font-weight:600;letter-spacing:-.01em}.panel-head span{color:var(--quiet);font:10px var(--mono);text-transform:uppercase;letter-spacing:.1em}.panel-body{padding:18px}.alert-list{display:grid}.alert-row{display:grid;grid-template-columns:5px 68px minmax(0,1fr) auto;gap:14px;align-items:center;padding:14px 18px;border-bottom:1px solid rgba(32,48,58,.7);transition:.18s}.alert-row:last-child{border-bottom:0}.alert-row:hover{background:rgba(255,255,255,.025)}.alert-bar{height:38px;border-radius:4px;background:var(--blue)}.alert-bar.critical{background:var(--red);box-shadow:0 0 16px rgba(255,107,107,.22)}.alert-bar.alert{background:var(--amber)}.score{font:24px var(--mono);letter-spacing:-.08em}.score small{display:block;color:var(--quiet);font:9px var(--mono);letter-spacing:.08em}.alert-main{min-width:0}.alert-main strong{display:block;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.alert-main .sub{display:flex;gap:9px;align-items:center;margin-top:4px;color:var(--muted);font-size:12px}.alert-main a,.token-link{color:var(--blue);text-decoration:none}.alert-main a:hover,.token-link:hover{text-decoration:underline}.time{color:var(--quiet);font:11px var(--mono);white-space:nowrap}.badge{display:inline-flex;align-items:center;padding:3px 7px;border:1px solid var(--line-bright);border-radius:4px;color:var(--muted);font:10px var(--mono);white-space:nowrap}.badge.critical{color:var(--red);border-color:rgba(255,107,107,.4);background:rgba(255,107,107,.08)}.badge.alert{color:var(--amber);border-color:rgba(245,184,75,.4);background:rgba(245,184,75,.08)}.badge.info{color:var(--blue)}.badge.confirmed{color:var(--mint);border-color:rgba(87,230,173,.35)}.badge.pending{color:var(--amber)}.chain-list{display:grid;gap:12px}.chain{display:flex;justify-content:space-between;align-items:center;padding:12px 0;border-bottom:1px solid var(--line)}.chain:last-child{border-bottom:0}.chain-name{display:flex;align-items:center;gap:10px}.chain-mark{width:27px;height:27px;border-radius:7px;background:var(--surface-3);display:grid;place-items:center;font:11px var(--mono);color:var(--blue)}.chain small{display:block;color:var(--quiet);font-size:11px;margin-top:2px}.chain-value{text-align:right;font:11px var(--mono);color:var(--muted)}.chain-value b{display:block;color:var(--mint);font-weight:400}.table-wrap{overflow:auto}.table{width:100%;border-collapse:collapse;min-width:680px}.table th{padding:11px 18px;text-align:left;color:var(--quiet);font:10px var(--mono);letter-spacing:.1em;text-transform:uppercase;font-weight:400;border-bottom:1px solid var(--line)}.table td{padding:14px 18px;border-bottom:1px solid rgba(32,48,58,.7);color:var(--muted);font-size:13px}.table tr:hover td{background:rgba(255,255,255,.025);color:var(--text)}.table td strong{color:var(--text);font-weight:550}.mono{font-family:var(--mono)}.num{text-align:right;font-family:var(--mono)}.toolbar{display:flex;gap:9px;align-items:center;margin-bottom:15px}.search{flex:1;max-width:350px;padding:10px 12px;border:1px solid var(--line);border-radius:6px;background:var(--surface);color:var(--text);outline:none}.search:focus{border-color:var(--mint);box-shadow:0 0 0 3px rgba(87,230,173,.08)}.tabs{display:flex;gap:3px;border-bottom:1px solid var(--line);margin-bottom:18px}.tab{border:0;background:transparent;color:var(--muted);padding:10px 14px;border-bottom:2px solid transparent}.tab.active{color:var(--mint);border-color:var(--mint)}.empty{padding:42px 18px;text-align:center;color:var(--quiet);font-family:var(--mono);font-size:11px}.token-hero{display:grid;grid-template-columns:1fr auto;gap:20px;align-items:center;padding:25px 27px;border:1px solid var(--line);border-radius:8px;background:radial-gradient(500px 180px at 0 0,rgba(87,230,173,.1),transparent 70%),var(--surface);margin-bottom:18px}.token-hero h1{margin:6px 0;font-size:29px;letter-spacing:-.04em}.address{font:12px var(--mono);color:var(--muted);word-break:break-all}.hero-score{text-align:right}.hero-score strong{display:block;font:48px var(--mono);letter-spacing:-.1em;color:var(--amber)}.hero-score span{color:var(--quiet);font:10px var(--mono);text-transform:uppercase;letter-spacing:.12em}.detail-grid{display:grid;grid-template-columns:minmax(0,1.3fr) minmax(300px,.7fr);gap:18px}.detail-grid .wide{grid-column:1/-1}.kv{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.kv div{padding:12px;border:1px solid var(--line);border-radius:6px;background:rgba(17,25,34,.6)}.kv label{display:block;color:var(--quiet);font:10px var(--mono);text-transform:uppercase;letter-spacing:.08em}.kv b{display:block;margin-top:7px;color:var(--text);font:14px var(--mono);word-break:break-all}.bars{display:grid;gap:11px}.bar-line{display:grid;grid-template-columns:112px 1fr 58px;gap:10px;align-items:center;color:var(--muted);font-size:12px}.bar-track{height:8px;background:var(--surface-3);border-radius:2px;overflow:hidden}.bar-fill{height:100%;background:var(--mint);border-radius:2px}.bar-line b{text-align:right;color:var(--text);font:11px var(--mono)}.graph{min-height:300px;display:grid;place-items:center;background:radial-gradient(circle at center,rgba(87,230,173,.08),transparent 48%),repeating-radial-gradient(circle at center,transparent 0 47px,rgba(87,230,173,.1) 48px 49px)}.graph-inner{text-align:center}.graph-core{width:78px;height:78px;border-radius:50%;border:1px solid var(--mint);display:grid;place-items:center;color:var(--mint);font:11px var(--mono);margin:auto;box-shadow:0 0 32px rgba(87,230,173,.15)}.graph-inner p{color:var(--muted);font-size:12px;margin:14px 0 0}.event-list{display:grid}.event{display:flex;gap:10px;align-items:center;padding:11px 0;border-bottom:1px solid var(--line);font-size:12px}.event:last-child{border-bottom:0}.event time{color:var(--quiet);font:10px var(--mono);min-width:70px}.event .event-type{color:var(--blue);font:10px var(--mono)}.event .event-hash{margin-left:auto;color:var(--muted);font:10px var(--mono)}.footnote{color:var(--quiet);font-size:11px;margin-top:12px}.mobile-menu{display:none}
@media(max-width:980px){.app{grid-template-columns:70px minmax(0,1fr)}.sidebar{padding:20px 10px}.brand{padding:0 9px 32px}.brand strong,.brand small,.nav-label,.nav button span,.side-bottom{display:none}.nav button{justify-content:center;padding:12px}.split,.detail-grid{grid-template-columns:1fr}.content{padding:26px 23px 50px}.topbar{padding:14px 23px}.token-hero{grid-template-columns:1fr}.hero-score{text-align:left}.hero-score strong{font-size:38px}}
@media(max-width:620px){.app{display:block}.sidebar{position:fixed;bottom:0;top:auto;left:0;right:0;height:62px;z-index:8;padding:7px 10px;border-right:0;border-top:1px solid var(--line);flex-direction:row}.brand,.nav-label,.side-bottom{display:none}.nav{width:100%;display:flex;justify-content:space-around}.nav button{flex:1;justify-content:center;padding:8px}.nav button.active{background:rgba(87,230,173,.1)}.main{padding-bottom:60px}.topbar{height:auto;padding:13px 16px}.top-actions .icon-btn{display:none}.content{padding:22px 16px 40px}.view-head{display:block}.view-head h1{font-size:25px}.actions{margin-top:16px}.metrics{grid-template-columns:repeat(2,1fr)}.metric{min-height:90px;padding:13px}.metric strong{font-size:23px}.alert-row{grid-template-columns:4px 52px minmax(0,1fr);gap:10px;padding:13px 12px}.alert-row>.time{grid-column:3}.score{font-size:20px}.panel-head{padding:14px}.panel-body{padding:14px}.kv{grid-template-columns:1fr 1fr}.bar-line{grid-template-columns:90px 1fr 48px}.table th,.table td{padding-left:12px;padding-right:12px}.eyebrow{font-size:9px}.page-title{font-size:17px}}
@media(prefers-reduced-motion:reduce){*,*:before,*:after{animation-duration:.01ms!important;transition-duration:.01ms!important}html{scroll-behavior:auto}}
</style>
</head>
<body>
<a class="skip" href="#main">Skip to content</a>
<div class="app">
<aside class="sidebar">
  <div class="brand"><div class="eye" aria-hidden="true"></div><div><strong>ARGUS</strong><small>intelligence console</small></div></div>
  <div class="nav-label">Workspace</div>
  <nav class="nav" aria-label="Primary navigation">
    <button data-route="overview" class="active"><i>⌂</i><span>Command center</span></button>
    <button data-route="alerts"><i>!</i><span>Alert inbox</span></button>
    <button data-route="watchlist"><i>◈</i><span>Watchlist</span></button>
     <button data-route="activity"><i>≋</i><span>Activity log</span></button>
     <button data-route="performance"><i>↗</i><span>Positions</span></button>
  </nav>
  <div class="nav-label" style="margin-top:28px">Inspect</div>
  <nav class="nav"><button data-route="health"><i>⊙</i><span>System health</span></button></nav>
</aside>
<div class="main">
<header class="topbar"><div class="crumb"><span class="eyebrow">ARGUS /</span><span class="page-title" id="pageTitle">Command center</span></div><div class="top-actions"><div class="status" id="connection"><i class="dot"></i><span>connecting</span></div><button class="icon-btn" id="refreshBtn" title="Refresh data">↻</button></div></header>
<main id="main" class="content">
<section class="view active" id="overviewView">
  <div class="view-head"><div><p>Evidence-first monitoring across the wallets, tokens, and liquidity surfaces you are watching.</p></div><div class="actions"><span class="badge confirmed">● live stream</span><span class="eyebrow" id="clock"></span></div></div>
  <div class="metrics"><div class="metric"><span class="label">Active watches</span><strong id="metricTokens">—</strong><span class="delta">tokens under observation</span></div><div class="metric alert"><span class="label">Active alerts</span><strong id="metricAlerts">—</strong><span class="delta">latest unretracted alert per token</span></div><div class="metric live"><span class="label">Signals today</span><strong id="metricSignals">—</strong><span class="delta">last 24 hours</span></div><div class="metric"><span class="label">Chain events</span><strong id="metricEvents">—</strong><span class="delta">last 24 hours</span></div></div>
  <section class="panel funnel"><div class="panel-head"><h2>Token selection funnel</h2><span>candidate evidence</span></div><div id="candidateMetrics" class="panel-body"></div></section>
  <div class="split"><section class="panel"><div class="panel-head"><h2>Priority alerts</h2><span>newest first</span></div><div id="overviewAlerts" class="alert-list"></div></section><section class="panel"><div class="panel-head"><h2>Chain pulse</h2><span>ingestion health</span></div><div class="panel-body"><div id="chains" class="chain-list"></div></div></section></div>
  <div class="split" style="margin-top:18px"><section class="panel"><div class="panel-head"><h2>Watched tokens</h2><span><button class="filter" data-route="watchlist">view all →</button></span></div><div class="table-wrap"><table class="table"><thead><tr><th>Token</th><th>Source</th><th class="num">Last alert</th></tr></thead><tbody id="overviewTokens"></tbody></table></div></section><section class="panel"><div class="panel-head"><h2>Performance journal</h2><span>12h watch</span></div><div id="overviewPerformance" class="panel-body"></div></section></div>
</section>
<section class="view" id="alertsView"><div class="view-head"><div><div class="eyebrow">Investigation queue</div><h1>Alert inbox</h1><p>Every alert is a lead with a score, rule trail, and block-level evidence.</p></div></div><div class="toolbar"><input class="search" id="alertSearch" placeholder="Filter by token or headline" /><button class="filter alert-filter active" data-filter="all">All</button><button class="filter alert-filter" data-filter="critical">Critical</button><button class="filter alert-filter" data-filter="alert">Alert</button></div><section class="panel"><div id="allAlerts" class="alert-list"></div></section></section>
 <section class="view" id="watchlistView"><div class="view-head"><div><div class="eyebrow">Monitored surfaces</div><h1>Watchlist</h1><p>Tokens promoted into live monitoring. Candidates remain separate until the evidence gates pass.</p></div></div><div class="tabs"><button class="tab active" data-watch="active">Active watches</button><button class="tab" data-watch="candidates">Candidates</button></div><section class="panel"><div class="table-wrap"><table class="table"><thead><tr><th>Token</th><th>Chain</th><th>Source / status</th><th class="num">Score</th><th class="num">Supply</th><th class="num">Last alert</th></tr></thead><tbody id="watchTable"></tbody></table></div></section></section>
<section class="view" id="activityView"><div class="view-head"><div><div class="eyebrow">Raw signal stream</div><h1>Activity log</h1><p>Normalized chain facts and rule signals, kept separate from the alert decision.</p></div></div><div class="split"><section class="panel"><div class="panel-head"><h2>Signals</h2><span>rule output</span></div><div id="signalsList" class="panel-body"></div></section><section class="panel"><div class="panel-head"><h2>Events</h2><span>chain facts</span></div><div id="eventsList" class="panel-body"></div></section></div></section>
 <section class="view" id="performanceView"><div class="view-head"><div><div class="eyebrow">Alert-linked observation</div><h1>Positions</h1><p>Pool-relative performance watches. These are observational, not executed trades.</p></div></div><section class="panel"><div class="panel-head"><h2>Performance watches</h2><span>12h window · +50% / -20%</span></div><div id="performanceList" class="panel-body"></div></section></section>
 <section class="view" id="healthView"><div class="view-head"><div><p>Know whether a quiet dashboard means a quiet chain or a stalled pipeline.</p></div></div><div class="metrics"><div class="metric live"><span class="label">Transport</span><strong id="healthTransport">—</strong><span class="delta">WebSocket state</span></div><div class="metric"><span class="label">Current head</span><strong id="healthHead">—</strong><span class="delta">latest observed block</span></div><div class="metric"><span class="label">Chains</span><strong id="healthChains">—</strong><span class="delta">configured networks</span></div><div class="metric"><span class="label">Refresh</span><strong>10s</strong><span class="delta">polling interval</span></div></div><section class="panel"><div class="panel-head"><h2>Chain diagnostics</h2><span>runtime status</span></div><div id="healthDetails" class="panel-body"></div></section></section>
<section class="view" id="tokenView"><div id="tokenContent"></div></section>
</main></div></div>
<script>
const $=id=>document.getElementById(id), state={tokens:[],alerts:[],signals:[],events:[],performance:[],candidates:[],metrics:null,status:null,watchMode:"active"};
const esc=s=>String(s??"").replace(/[&<>\"]/g,c=>c==="&"?"&amp;":c==="<"?"&lt;":c===">"?"&gt;":"&quot;");
const short=a=>a?(String(a).slice(0,8)+"…"+String(a).slice(-5)):"—";
const num=v=>{if(v===null||v===undefined||v==="")return "—";const n=Number(v);if(!isFinite(n))return esc(v);if(n>=1e15)return(n/1e15).toFixed(2)+"T";if(n>=1e12)return(n/1e12).toFixed(2)+"T";if(n>=1e9)return(n/1e9).toFixed(2)+"B";if(n>=1e6)return(n/1e6).toFixed(2)+"M";if(n>=1e3)return(n/1e3).toFixed(1)+"K";return n.toLocaleString()};
const ago=s=>{if(!s)return"—";const d=Math.max(0,Math.floor(Date.now()/1000)-Number(s));return d<60?d+"s":d<3600?Math.floor(d/60)+"m":d<86400?Math.floor(d/3600)+"h":Math.floor(d/86400)+"d"};
const sev=a=>a.severity||"info";
function tokenLink(chain,address){return address?'<a class="token-link" href="#/token/'+(chain||1)+'/'+address+'">'+esc(short(address))+'</a>':"—"}
 function alertRow(a){let p={};try{p=JSON.parse(a.payload_json||"{}")}catch{}const level=sev(a),perf=a.performance_status&&a.performance_status!=="pending"?'<span class="badge">'+esc(String(a.performance_status).replaceAll("_"," "))+'</span>':"";return '<div class="alert-row"><i class="alert-bar '+level+'"></i><div class="score">'+esc(a.score||"—")+'<small>SCORE</small></div><div class="alert-main"><strong>'+esc(p.headline||a.headline||"Detection event")+'</strong><div class="sub"><span class="badge '+level+'">'+esc(level)+'</span>'+tokenLink(a.chain_id||a.chainId,a.token_address||a.tokenAddress)+'<span>· '+(a.confirmed?'<span class="badge confirmed">confirmed</span>':'<span class="badge pending">unconfirmed</span>')+'</span>'+perf+'</div></div><time class="time">'+ago(a.created_at||(Date.now()/1000))+' ago</time></div>'}
function empty(text){return'<div class="empty">'+text+'</div>'}
function renderAlerts(target,rows){$(target).innerHTML=rows.length?rows.slice(0,40).map(alertRow).join(""):empty("No alerts in this view")}
function renderChains(status){const cObj=status&&status.chains||{};const chains=Array.isArray(cObj)?cObj:Object.entries(cObj).map(([k,v])=>({chainId:k,...v}));$("chains").innerHTML=chains.length?chains.map(c=>'<div class="chain"><div class="chain-name"><i class="chain-mark">'+esc(String(c.chainId||c.chain_id||"?").slice(0,2))+'</i><div><strong>'+esc(c.name||("Chain "+(c.chainId||c.chain_id)))+'</strong><small>'+esc(c.state||c.status||"observing")+'</small></div></div><div class="chain-value"><b>● '+esc(c.state||c.status||"ready")+'</b><span>head '+esc(c.lastHead||c.head||c.block||"—")+'</span></div></div>').join(""):empty("No chain status available")}
function renderTokens(rows,target){$(target).innerHTML=rows.length?rows.slice(0,30).map(t=>'<tr><td><strong>'+esc(t.symbol||"Unknown")+'</strong><br>'+tokenLink(t.chainId,t.address)+'</td><td><span class="badge">'+esc(t.source||"manual")+'</span></td><td class="num">'+(t.lastAlert?ago(t.lastAlert.created_at)+" ago":"—")+'</td></tr>').join(""): '<tr><td colspan="3">'+empty("No active token watches")+'</td></tr>'}
function renderPerformance(rows){if(!rows.length){$("overviewPerformance").innerHTML=empty("No alert outcomes yet");return}const active=rows.filter(x=>x.outcome==="active").length,win=rows.filter(x=>x.outcome==="target_hit").length,loss=rows.filter(x=>x.outcome==="stop_hit").length;$("overviewPerformance").innerHTML='<div class="kv"><div><label>Active</label><b>'+active+'</b></div><div><label>Targets</label><b style="color:var(--mint)">'+win+'</b></div><div><label>Stops</label><b style="color:var(--red)">'+loss+'</b></div></div><p class="footnote">Pool-relative observation. Not an executed trade.</p>'}
 function renderActivity(){const signals=state.signals||[],events=state.events||[];$("signalsList").innerHTML=signals.length?'<div class="event-list">'+signals.slice(0,40).map(s=>'<div class="event"><time>'+ago(s.timestamp||s.created_at)+' ago</time><span class="event-type">'+esc(s.ruleId||s.rule_id||"signal")+'</span>'+tokenLink(s.chainId||s.chain_id,s.tokenAddress||s.token_address)+'<span class="event-hash">score '+esc(s.score??"—")+' · w'+esc(s.weight||0)+' · b'+esc(s.blockNumber||s.block_number||0)+'</span><span class="badge '+(s.outcome==="alert_created"?"confirmed":s.outcome==="alert_suppressed"?"pending":"")+'">'+esc(s.outcome==="below_threshold"?"below threshold":s.outcome==="alert_created"?"alert created":s.outcome==="alert_suppressed"?"suppressed: "+(s.outcomeReason||"unknown"):"evaluating")+'</span></div>').join("")+'</div>':empty("No signals yet");$("eventsList").innerHTML=events.length?'<div class="event-list">'+events.slice(0,40).map(e=>'<div class="event"><time>b'+esc(e.block_number||e.blockNumber||0)+'</time><span class="event-type">'+esc(e.type||e.kind||"event")+'</span><span class="event-hash">'+esc(short(e.tx_hash||e.txHash))+'</span></div>').join("")+'</div>':empty("No events yet")}
  function renderPositions(){const rows=state.performance||[];$("performanceList").innerHTML=rows.length?'<div class="event-list">'+rows.slice(0,50).map(p=>'<div class="event"><time>'+esc(p.outcome||"active")+'</time>'+tokenLink(p.chain_id,p.token_address)+'<span class="event-type">entry '+esc(p.entry_price)+'</span><span class="event-hash">current '+esc(p.current_price)+' · expires '+esc(new Date(Number(p.expires_at)*1000).toISOString())+'</span></div>').join('')+'</div>':empty("No performance watches yet. Watches open only for qualifying alerts with fresh, priced swaps.")}
 function renderWatchlist(){const rows=state.watchMode==="candidates"?state.candidates||[]:state.tokens;$("watchTable").innerHTML=rows.length?rows.map(t=>'<tr><td><strong>'+esc(t.symbol||"Candidate")+'</strong><br>'+tokenLink(t.chainId,t.address||t.token_address)+'</td><td>'+esc(t.chainId||t.chain_id||"—")+'</td><td><span class="badge '+(state.watchMode==="candidates"?"pending":"")+'">'+esc(state.watchMode==="candidates"?(t.status||t.source||"candidate"):(t.source||"manual"))+'</span></td><td class="num">'+(state.watchMode==="candidates"?esc(t.score??"—"):"—")+'</td><td class="num">'+num(t.totalSupply)+'</td><td class="num">'+(t.lastAlert?ago(t.lastAlert.created_at)+" ago":"—")+'</td></tr>').join(""): '<tr><td colspan="6">'+empty(state.watchMode==="candidates"?"No pending candidates":"No active watches")+'</td></tr>'}
function renderHealth(){const cObj=state.status&&state.status.chains||{};const chains=Array.isArray(cObj)?cObj:Object.entries(cObj).map(([k,v])=>({chainId:k,...v}));$('healthTransport').textContent=$("connection").className.includes("live")?"LIVE":"STALE";$('healthHead').textContent=chains.map(c=>c.lastHead||c.head||c.block).filter(Boolean).join(" / ")||"—";$('healthChains').textContent=chains.length||"—";$('healthDetails').innerHTML=chains.length?chains.map(c=>'<div class="chain"><div class="chain-name"><i class="chain-mark">#</i><div><strong>'+esc(c.name||("Chain "+c.chainId))+'</strong><small>transport '+esc(c.transport||"websocket")+'</small></div></div><div class="chain-value"><b>'+esc(c.state||c.status||"ready")+'</b><span>head '+esc(c.lastHead||c.head||c.block||"—")+'</span></div></div>').join(""):empty("No diagnostics available")}
function renderCandidateMetrics(){const c=state.metrics&&state.metrics.candidates;if(!c){$("candidateMetrics").innerHTML=empty("Waiting for candidate metrics");return}$("candidateMetrics").innerHTML='<div class="kv"><div><label>Discovered</label><b>'+c.total+'</b></div><div><label>Evaluated</label><b>'+c.evaluated+'</b></div><div><label>Eligible</label><b style="color:var(--mint)">'+c.eligible+'</b></div><div><label>Promoted</label><b style="color:var(--blue)">'+c.promoted+'</b></div><div><label>Promotion rate</label><b>'+c.promotionRate+'%</b></div><div><label>Evaluation completion</label><b>'+c.evaluationCompletionRate+'%</b></div></div><p class="footnote">Rejected '+c.rejected+' · expired '+c.expired+' · evaluating '+c.evaluating+'</p>'}
let loadTimer;
async function load(){
  clearTimeout(loadTimer);
  loadTimer = setTimeout(async () => {
    try{
      const res=await fetch("/api/snapshot");
      if(!res.ok) throw new Error("API error");
      const snapshot=await res.json();
      state.status=snapshot.status;state.metrics=snapshot.metrics;state.tokens=snapshot.tokens;state.alerts=snapshot.alerts;state.signals=snapshot.signals;state.events=snapshot.events;state.performance=snapshot.performance;state.candidates=snapshot.candidates;
      $("metricTokens").textContent=state.metrics.activeWatches;$("metricAlerts").textContent=state.metrics.openAlerts;$("metricSignals").textContent=state.metrics.signalsToday;$("metricEvents").textContent=state.metrics.events24h;
      renderCandidateMetrics();renderAlerts("overviewAlerts",state.alerts);renderChains(state.status);renderTokens(state.tokens,"overviewTokens");renderPerformance(state.performance);renderActivity();renderWatchlist();renderHealth();
      setConnection("live","engine online");
    }catch(e){
      setConnection("down","engine offline");
    }
  }, 250);
}
function setConnection(kind,text){const el=$("connection");el.className="status "+kind;el.querySelector("span").textContent=text}
 function route(){const raw=location.hash.replace(/^#\\/?/,"")||"overview", parts=raw.split("/");document.querySelectorAll(".view").forEach(v=>v.classList.remove("active"));document.querySelectorAll(".nav button").forEach(b=>b.classList.toggle("active",b.dataset.route===parts[0]));const titles={overview:"Command center",alerts:"Alert inbox",watchlist:"Watchlist",activity:"Activity log",health:"System health"};if(parts[0]==="token"&&parts[2]){$("tokenView").classList.add("active");$("pageTitle").textContent="Token investigation";openToken(parts[1],parts[2]);return}const view=$(parts[0]+"View")||$("overviewView");view.classList.add("active");$("pageTitle").textContent=titles[parts[0]]||titles.overview;if(parts[0]==="alerts")renderAlerts("allAlerts",state.alerts);if(parts[0]==="watchlist")renderWatchlist()}
async function openToken(chain,address){const root=$("tokenContent");root.innerHTML='<div class="empty">Loading investigation surface…</div>';try{const r=await Promise.all([fetch("/api/token/"+chain+"/"+address),fetch("/api/graph/token/"+chain+"/"+address)]);for(const res of r) if(!res.ok) throw new Error("API error");const data=await r[0].json(),graph=await r[1].json(),t=data.token||{};const clusters=graph.clusters||[],largest=clusters.slice().sort((a,b)=>(b.pctOfSupply||0)-(a.pctOfSupply||0))[0],score=data.alerts&&data.alerts[0]?data.alerts[0].score:"—";root.innerHTML='<div class="view-head"><div><div class="eyebrow">Token investigation / chain '+esc(chain)+'</div><h1>'+esc(t.symbol||short(address))+'</h1><p>Trace ownership concentration, funding links, and alert evidence without leaving the case.</p></div><div class="actions"><button class="filter" data-route="watchlist">← Watchlist</button></div></div><div class="token-hero"><div><div class="eyebrow">Contract address</div><div class="address">'+esc(address)+'</div><h1>'+esc(t.symbol||"Unknown token")+'</h1><span class="badge">'+esc(t.source||"manual")+'</span></div><div class="hero-score"><strong>'+esc(score)+'</strong><span>latest risk score</span></div></div><div class="detail-grid"><section class="panel"><div class="panel-head"><h2>Token facts</h2><span>observed metadata</span></div><div class="panel-body"><div class="kv"><div><label>Total supply</label><b>'+num(t.totalSupply)+'</b></div><div><label>Decimals</label><b>'+esc(t.decimals??graph.decimals??"—")+'</b></div><div><label>Tracked supply</label><b>'+num(graph.circulatingSupply)+'</b></div></div></div></section><section class="panel"><div class="panel-head"><h2>Concentration</h2><span>cluster share</span></div><div class="panel-body"><div class="bars">'+(clusters.length?clusters.slice(0,5).map((c,i)=>'<div class="bar-line"><span>cluster '+(i+1)+'</span><div class="bar-track"><i class="bar-fill" style="width:'+Math.min(100,Number(c.pctOfSupply)||0)+'%;background:'+(i?'var(--blue)':'var(--mint)')+'"></i></div><b>'+Number(c.pctOfSupply||0).toFixed(2)+'%</b></div>').join(""):empty("No clusters tracked"))+'</div></div></section><section class="panel"><div class="panel-head"><h2>Funding constellation</h2><span>'+((graph.funding||[]).length)+' links</span></div><div class="graph"><div class="graph-inner"><div class="graph-core">'+(largest?largest.memberCount:0)+' wallets</div><p>largest cluster · '+(largest?Number(largest.pctOfSupply||0).toFixed(2):"0")+'% of supply</p></div></div></section><section class="panel"><div class="panel-head"><h2>Alert history</h2><span>case trail</span></div><div class="panel-body">'+((data.alerts||[]).length?(data.alerts||[]).map(alertRow).join(""):empty("No alerts for this token"))+'</div></section><section class="panel wide"><div class="panel-head"><h2>Liquidity pools</h2><span>registered venues</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Pool</th><th>Quote token</th><th>Factory</th></tr></thead><tbody>'+((data.pools||[]).length?(data.pools||[]).map(p=>'<tr><td class="mono">'+esc(short(p.pool_address))+'</td><td class="mono">'+esc(short(p.quote_token))+'</td><td><span class="badge">'+esc(p.factory)+'</span></td></tr>').join(""):'<tr><td colspan="3">'+empty("No pools registered")+'</td></tr>')+'</tbody></table></div></section></div>'}catch(e){root.innerHTML='<div class="empty" style="color:var(--red)">Could not load token investigation surface.</div>'}}
document.querySelectorAll("[data-route]").forEach(b=>b.addEventListener("click",()=>{location.hash="#/"+b.dataset.route}));$("refreshBtn").addEventListener("click",load);document.querySelectorAll(".alert-filter").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".alert-filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");const q=$("alertSearch").value.toLowerCase(),f=b.dataset.filter;renderAlerts("allAlerts",state.alerts.filter(a=>(f==="all"||sev(a)===f)&&(!q||(a.token_address||"").toLowerCase().includes(q)||String(a.payload_json||"").toLowerCase().includes(q))))}));$("alertSearch").addEventListener("input",()=>document.querySelector(".alert-filter.active").click());document.querySelectorAll("[data-watch]").forEach(b=>b.addEventListener("click",()=>{state.watchMode=b.dataset.watch;document.querySelectorAll("[data-watch]").forEach(x=>x.classList.toggle("active",x===b));renderWatchlist()}));window.addEventListener("hashchange",route);setInterval(()=>$("clock").textContent=new Date().toISOString().slice(11,19)+" UTC",1000);let ws;function connect(){try{ws=new WebSocket((location.protocol==="https:"?"wss:":"ws:")+"//"+location.host+"/ws");ws.onopen=()=>{setConnection("live","engine online");load()};ws.onclose=()=>{setConnection("stale","reconnecting");setTimeout(connect,2000)};ws.onerror=()=>setConnection("stale","reconnecting");ws.onmessage=(e)=>{try{const p=JSON.parse(e.data);if(p.type==="status"){state.status=p.status;renderChains(state.status);renderHealth()}else if(p.type==="alert"){state.alerts.unshift(p);if(state.alerts.length>100)state.alerts.pop();$("metricAlerts").textContent=state.alerts.length;renderAlerts("overviewAlerts",state.alerts);if(location.hash.startsWith("#/alerts"))renderAlerts("allAlerts",state.alerts)}else if(p.type==="event"){state.events.unshift(p.data);if(state.events.length>100)state.events.pop();$("metricEvents").textContent=state.events.length;if(location.hash.startsWith("#/activity"))renderActivity()}else if(p.type==="signal"){state.signals.unshift(p.data);if(state.signals.length>100)state.signals.pop();$("metricSignals").textContent=state.signals.length;if(location.hash.startsWith("#/activity"))renderActivity()}else if(p.type==="performance"){state.performance.unshift(p.data);renderPerformance(state.performance)}}catch(err){}}}catch(e){setConnection("down","engine offline")}}route();load();connect();
setInterval(load,10000);
 function openToken(chain,address){const root=$("tokenContent");root.innerHTML=empty("Loading persisted token evidence…");Promise.all([fetch("/api/token/"+chain+"/"+address),fetch("/api/graph/token/"+chain+"/"+address)]).then(async ([detailRes,graphRes])=>{if(!detailRes.ok||!graphRes.ok)throw new Error("API error");const data=await detailRes.json(),graph=await graphRes.json(),t=data.token||{},a=data.availability||{},summary=data.recentEventSummary||{},events=data.recentEvents||[],clusters=graph.clusters||[],largest=clusters.slice().sort((x,y)=>(y.pctOfSupply||0)-(x.pctOfSupply||0))[0],status=(label,available)=>'<span class="badge '+(available?'confirmed':'pending')+'">'+(available?'available':'not available')+'</span> <span>'+label+'</span>',availability='<section class="panel wide"><div class="panel-head"><h2>Data availability</h2><span>persisted evidence only</span></div><div class="panel-body"><div class="kv"><div><label>Metadata</label><b>'+status("token record",!!a.metadata)+'</b></div><div><label>Graph</label><b>'+status("live projection",!!a.graph)+'</b></div><div><label>History</label><b>'+status("alerts / signals / events",!!a.history)+'</b></div><div><label>Recent events</label><b>'+status(String(summary.count||0)+" stored",!!a.events)+'</b></div></div></div></section>',eventRows=events.length?events.map(e=>'<div class="event"><time>b'+esc(e.block_number)+' · '+esc(e.finalized?'finalized':'unconfirmed')+'</time><span class="event-type">'+esc(e.type)+'</span><span class="event-hash">'+esc(short(e.tx_hash))+'</span></div>').join(""):empty("No persisted events for this token yet");root.innerHTML='<div class="view-head"><div><div class="eyebrow">Token investigation / chain '+esc(chain)+'</div><h1>'+esc(t.symbol||short(address))+'</h1><p>Read-only investigation surface. Missing projections are reported, not backfilled by this request.</p></div><div class="actions"><button class="filter" data-route="watchlist">← Watchlist</button></div></div>'+availability+'<div class="token-hero"><div><div class="eyebrow">Contract address</div><div class="address">'+esc(address)+'</div><h1>'+esc(t.symbol||"Metadata unavailable")+'</h1><span class="badge">'+esc(t.source||"unwatched")+'</span></div><div class="hero-score"><strong>'+esc(data.alerts&&data.alerts[0]?data.alerts[0].score:"—")+'</strong><span>latest risk score</span></div></div><div class="detail-grid"><section class="panel"><div class="panel-head"><h2>Token facts</h2><span>observed metadata</span></div><div class="panel-body"><div class="kv"><div><label>Total supply</label><b>'+num(t.totalSupply)+'</b></div><div><label>Decimals</label><b>'+esc(t.decimals??graph.decimals??"—")+'</b></div><div><label>Tracked supply</label><b>'+num(graph.circulatingSupply)+'</b></div></div></div></section><section class="panel"><div class="panel-head"><h2>Concentration</h2><span>cluster share</span></div><div class="panel-body">'+(clusters.length?clusters.slice(0,5).map((c,i)=>'<div class="bar-line"><span>cluster '+(i+1)+'</span><div class="bar-track"><i class="bar-fill" style="width:'+Math.min(100,Number(c.pctOfSupply)||0)+'%;background:'+(i?'var(--blue)':'var(--mint)')+'"></i></div><b>'+Number(c.pctOfSupply||0).toFixed(2)+'%</b></div>').join(""):empty("Graph projection unavailable; no clusters have been observed in this process."))+'</div></section><section class="panel wide"><div class="panel-head"><h2>Recent persisted events</h2><span>'+esc(String(summary.count||0))+' retained</span></div><div class="panel-body"><div class="event-list">'+eventRows+'</div></div></section></div>'}).catch(()=>{root.innerHTML='<div class="empty" style="color:var(--red)">Could not load persisted token evidence. The live engine was not backfilled.</div>'})}
  document.querySelector('[data-route="performance"]')?.addEventListener('click',()=>{setTimeout(()=>{$("pageTitle").textContent="Positions";renderPositions()},0)});setInterval(()=>{if($("performanceView")?.classList.contains("active"))renderPositions()},1000);
  </script>
</body></html>`;
}
